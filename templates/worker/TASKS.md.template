# {{WORKER_TITLE}} Tasks

## 总目标

{{GOAL}}

## 验收条件

- [ ] 每轮最多处理一个最小任务。
- [ ] 每轮修改范围只限授权目录。
- [ ] 每轮运行验证命令或明确记录未验证原因。
- [ ] 每轮更新 `STATE.md` 和 `HANDOFF.md`。
- [ ] 连续失败达到上限后停止修改，只汇报阻塞。

## 任务队列

### 阶段 1：初始化检查

- [ ] 确认目标目录存在：`{{OUTPUT_DIR}}`
- [ ] 确认 worker 状态文件存在。
- [ ] 运行验证命令：`{{VALIDATION_COMMAND}}`

### 阶段 2：主体任务推进

按顺序推进，每轮只处理一个最小任务：

{{TASK_QUEUE}}

### 阶段 3：质量检查

- [ ] 检查输出是否符合目标。
- [ ] 检查是否有重复、遗漏、格式错误或待人工补充内容。
- [ ] 把跨轮问题记录到 review 文件或 `HANDOFF.md`。

## 明确禁止

- 不访问网络，除非本文件后续明确授权。
- 不调用外部 API，除非本文件后续明确授权。
- 不自动 push、发布或部署。
- 不删除未知文件。
- 不修改系统级配置或项目外文件。
- 不处理、不读取、不写入密钥、密码、token、cookie、私钥。
- 不自行启动新的 Claude 进程。
- 不进入无限循环。
- 不等待用户输入。

## 授权动作

- 允许读取当前项目内文件。
- 允许编辑：`{{OUTPUT_DIR}}`
- 允许编辑本 worker 状态文件：`STATE.md`、`HANDOFF.md`、`FAILURES.md`。
- 允许运行验证命令：`{{VALIDATION_COMMAND}}`
